#!/bin/sh
directory="$1"
generator="$2"
if ! type otel4netcat 1> /dev/null 2> /dev/null; then
  otel4netcat() { "$@"; }
fi
otel4netcat ncat -l -w 60 -k -c "sh /usr/share/abomhttp/serve '$directory' '$generator'"
