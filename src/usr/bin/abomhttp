#!/bin/sh
port="$1"
directory="$2"
generator="$3"
command=ncat
if type otel4netcat_http 1> /dev/null 2> /dev/null; then
  command="otel4netcat_http $command"
fi
export PATH="/usr/share/abomhttp:$PATH"
$command -l -k -i $((60 * 60))s -c "abomhttp_serve '$directory' '$generator'" "$port"
