#!/bin/sh
port="$1"
directory="$2"
generator="$3"
if ! type otel4netcat 1> /dev/null 2> /dev/null; then
  otel4netcat() { "$@"; }
fi
export PATH="/usr/share/abomhttp:$PATH"
otel4netcat ncat -l -k -c "abomhttp_serve '$directory' '$generator'" "$port"
